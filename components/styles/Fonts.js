import { Global } from '@emotion/react';

const Fonts = () => (
  <Global
    styles={`@import url('https://fonts.googleapis.com/css2?family=Montserrat&family=Overpass&family=Raleway&display=swap'););`}
  />
);

export default Fonts;
